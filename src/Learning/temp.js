/*
 let refs = useRef();
  useEffect(() => {
    console.log(refs);
    refs.current.style.backgroundImage = `url('${obj.img}')`;
  });
  return (
    <div ref={refs} className="text-blue-300 w-[80vw] ">
      <div className="">{obj.title}</div>
    </div>
  );
*/
